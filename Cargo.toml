[package]
name = "sh-http-server"
version = "0.1.0"
edition = "2021"


[dependencies]
actix-web = "4"
smart-house = { git = "https://github.com/inspired2/smart-house.git"}
serde = {version = "1", features = ["derive"]}

[dev-dependencies]
tokio = {version = "*", features = ["full"]}